# Ensure .DS_Store is in .gitignore
echo .DS_Store >> .gitignore

# Remove .DS_Store from Git's tracking
git rm --cached .DS_Store

# Commit the changes
git commit -m "Remove .DS_Store files from repository"

# Push changes to remote repository
git push
